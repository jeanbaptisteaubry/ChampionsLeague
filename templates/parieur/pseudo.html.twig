{% extends 'base.html.twig' %}
{% block body %}
  <h1>Changer le pseudo</h1>
  {% if ok %}<p class="flash-ok">{{ ok }}</p>{% endif %}
  {% if error %}<p class="flash-err">{{ error }}</p>{% endif %}
  <form method="post" action="/parieur/pseudo">
    <input type="hidden" name="_csrf" value="{{ app.csrf }}">
    <label for="pseudo">Nouveau pseudo</label>
    <input type="text" id="pseudo" name="pseudo" required value="{{ current }}" maxlength="50">

    <button type="submit">Mettre à jour</button>
  </form>
{% endblock %}

