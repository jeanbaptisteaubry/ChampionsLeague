{% extends 'base.html.twig' %}
{% block body %}
  <h1>Inscription à une campagne</h1>
  {% if ok %}<p class="flash-ok">{{ ok }}</p>{% endif %}
  {% if error %}<p class="flash-err">{{ error }}</p>{% endif %}
  <form method="post" action="/parieur/inscription">
    <input type="hidden" name="_csrf" value="{{ app.csrf }}">
    <label>Campagne
      <select name="idCampagnePari">
        {% for c in campagnes %}
          <option value="{{ c.idCampagnePari }}">{{ c.libelle }}</option>
        {% endfor %}
      </select>
    </label>
    <button>S'inscrire</button>
  </form>
{% endblock %}

